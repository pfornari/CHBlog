#ENVIADO POR TUTOR
############################

django-admin startproject PruebaAfter

py manage.py runserver

Crear la app
python manage.py startapp AppCoder

Chequear que se cargo la app
python manage.py check Aplicacion1After

Migrar proyecto
python manage.py migrate

Migrar modelos de las app
python manage.py makemigrations

Despues migrar modelos no olvidar volver a migrar con migrate


GIT
==============
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin "Link del repo"
git push -u origin main

git remote add origin "Link del repo"
git branch -M main
git push -u origin main
